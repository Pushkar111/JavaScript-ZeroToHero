// const
console.log(a);

// Before we initialize , it shows reference error [undefined]
// ReferenceError: a is not defined

// But when we initialize it will show reference error [cannot access 'a' before initialization]
// ReferenceError: Cannot access 'a' before initialization
const a = 9;

// ------------------------------------------------------------------------------------------

// below is second synario
const b = 10;
console.log(b); // 10
