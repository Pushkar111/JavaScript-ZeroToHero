console.log("======== scrollBy() ========");
console.log("window scrollBy() Object Method");

// Method	Description
/*

1. scrollBy() -> Scrolls the window by the specified number of pixels.
    ==> Syntax : window.scrollBy(x, y);
    ==> window.scrollBy(x, y);
*/

// 1. scrollBy()
// window.scrollBy(100, 100); // It will scroll the current window by 100px right and 100px down.