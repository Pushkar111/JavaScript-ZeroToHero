console.log("======== moveBy() ========");
console.log("window moveBy() Object Method");

// Method	Description
/*

1. moveBy() -> Moves the window by the specified pixels.
    ==> Syntax : window.moveBy(x, y);
    ==> window.moveBy(x, y);
*/

// 1. moveBy()
// window.moveBy(100, 100); // It will move the current window by 100px right and 100px down.
